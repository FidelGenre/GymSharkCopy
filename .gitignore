# --- SCRIPTS DE AUTOMATIZACIÓN (RAÍZ) ---
# Ignora todos los archivos de Python y SQL en la raíz
*.py
*.sql

# Opcional: Si querés ser ultra específico con los nombres que tenés
agrupar_archivos.py
limpiar_fotos.py
poblar_*.py
carga_*.sql

# --- BACKEND (SPRING BOOT) ---
# Ignora la configuración sensible
**/src/main/resources/application.properties

# Archivos de compilación y Maven
**/target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.restoreBackup
.mvn/wrapper/maven-wrapper.jar

# --- FRONTEND (REACT / VITE) ---
# Ignora dependencias y compilación
**/node_modules/
**/dist/
**/build/
.env
.env.local

# --- IDEs Y SISTEMA ---
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db
desktop.ini

# --- BASE DE DATOS (PostgreSQL) ---
# Ignora archivos locales de pgAdmin o backups
data/
pgdata/
*.backup
*.sql_dump